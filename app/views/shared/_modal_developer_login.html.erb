<div class="modal fade" id="login-modal-developer" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true" style="display: none;">
  <div class="modal-dialog">
    <div class="loginmodal-container">
      <div class="login-content">
        <h2 style="margin-top: 11px;">Log-in Developer</h2>
        <hr style="border-top: solid 1px #DADADA; width: 100%;">
        <%= simple_form_for(:developer, url: session_path(:developer)) do |f| %>
        <div class="form-inputs">
          <%= f.input :email, required: false, autofocus: true, label: false, placeholder: 'E-mail', :input_html => {:style=> 'text-align: center; margin: 0 auto;'} %>
          <%= f.input :password, required: false, label: false, placeholder: 'Password', :input_html => {:style=> 'text-align: center; margin: 0 auto;'} %>
        </div>


        <div class="form-actions">
          <%= f.button :submit, "Log in", class: "btn btn-success" %>
        </div>
          <%= f.input :remember_me, as: :boolean %>
        <% end %>

        <hr style="border-top: solid 1px #DADADA; width: 100%;">
        <ul class="list-inline options">
          <li>
            <%= link_to "Sign up", new_registration_path(:developer) %>
          </li>

          <li>
            <%= link_to "Forgot your password?", new_password_path(:developer) %>
          </li>
        </ul>
        <% # render "devise/shared/links", resource_name: :developer, devise_mapping: Devise.mappings[:developer] %>
      </div>
    </div>
  </div>
</div>
