<h1>list of your jobs</h1>

<div class="container">
  <div class="text-center">
    <% @jobs.each do |job| %>
    <%= job.title %>
    <%= job.description %>
    <%= job.budget %>
    <%= link_to "View", client_job_path(job) %>
    <% end %>
  </div>
</div>
